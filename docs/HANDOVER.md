# プロジェクト引き継ぎ・状況共有（HANDOVER）

このファイルは、複数の人間・AIエージェント間で現在の作業状況を引き継ぐためのドキュメントです。
作業を開始する前に必ず確認し、作業終了時には最新の状態に更新してください。

## 現在の作業状況
- [x] プロジェクト初期ドキュメント（計画書・指示テンプレ）の読み込みと理解
- [x] AIエージェント用共通ルール（`AGENTS.md`）のセットアップ
- [x] 各IDE/AI用設定ファイル（`.cursorrules`, `CLAUDE.md`, `.geminirules`）のエントリポイント化
- [x] 外部向け説明（`README.md`）の作成
- [x] Phase 1 の初期セットアップ（Next.jsプロジェクト生成、パッケージ追加など）
- [x] **Day 3〜5 のタスク（GAS層の実装）**:
  - `gas/Main.gs`, `gas/Auth.gs`, `gas/TasksRepo.gs`, `gas/Lock.gs` のコード生成。
  - スプレッドシート側のセットアップとGASへのプッシュ完了。
- [x] **Day 6〜10 のタスク（Next.js UI層の実装）**:
  - `src/lib/*`: 型定義、Auth、`taskMatcher`、`gasClient`
  - `src/app/api/*`: Gemini解析（`parse-report`）、GASプロキシ（`submit-task-update`）
  - `src/components/*`: タスクカード、AI確認画面など
  - `src/app/*`: ログイン画面、ダッシュボード、タスク一覧、AI報告画面

## 次のアクション（未完了タスク）
- **ユーザーによる手動確認（必須）**:
  - スプレッドシートにダミーのマスタデータ（Employees, Stores, Campaigns, Tasks）を入力する。
  - `npm run dev` でローカルサーバーを起動し、ブラウザで動作確認・UIテストを行う。
- **次の開発フェーズ（Phase 1.5 または 2）への移行**:
  - 画像付き報告機能の追加や、モバイルブラウザ向けの最適化など。

## 注意点
- **絶対に `AGENTS.md` の非交渉事項を守ること**。特にAIによるDBへの直接更新は行わない（必ず確認画面を経由）。
- 設計書と矛盾するコードは書かない。迷った場合は `docs/SV業務DX化_計画書_v1.0.md` を唯一の設計ソースとする。
